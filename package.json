{
  "name": "conto-auth",
  "version": "1.0.0",
  "description": "The main entrypoint for the conto app",
  "main": "index.js",
  "scripts": {
    "dev": "rollup -c -w",
    "start": "sirv public --dev --host 0.0.0.0 --port 3000",
    "compose:dev:up": "docker-compose watch",
    "compose:dev:down": "docker-compose down -v"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "svelte": "^4.2.9"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "rollup": "^4.9.6",
    "rollup-plugin-svelte": "^7.1.6",
    "sirv-cli": "^2.0.2"
  }
}
